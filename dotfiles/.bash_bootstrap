#!/bin/bash

# ------------
# CUSTOM CONFIGURATION
# ------------
# Load the rest of the dotfiles
for file in ~/.{bash_conf,bash_aliases,bash_prompt,path,exports,bash_gpg,misc,localrc}; do
	[ -f "$file" ] && source "$file"
done
unset file
